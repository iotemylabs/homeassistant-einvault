{
  "config": {
    "step": {
      "user": {
        "title": "Connect to EinVault",
        "description": "Point Home Assistant at your EinVault instance. Use the instance's local address (for example http://192.168.1.10:7387) rather than a public hostname — EinVault rate limits by client IP, and traffic arriving through a reverse proxy shares a single budget with every other consumer.",
        "data": {
          "url": "URL",
          "api_token": "API token"
        },
        "data_description": {
          "url": "Base URL of your EinVault instance, without a trailing path.",
          "api_token": "Create one under Settings → API tokens. Choose full access — a write-only token cannot read any data."
        }
      },
      "reauth_confirm": {
        "title": "Reauthenticate EinVault",
        "description": "The API token for {url} was rejected. If you rotated it, paste the new token here.",
        "data": {
          "api_token": "API token"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the EinVault instance. Check the address and that the container is running.",
      "invalid_auth": "The API token was rejected. Check that it was copied in full and has not been revoked.",
      "invalid_url": "That does not look like a valid URL. Include the scheme, for example http://192.168.1.10:7387.",
      "api_disabled": "The bearer API is turned off on this instance. Set API_TOKENS_ENABLED=true and restart the container.",
      "rate_limited": "EinVault is rate limiting requests right now. Wait a minute and try again.",
      "write_only_token": "This is a write-only token. It cannot read companions, logs, weight, or reminders, so no entities could be created. Create a token with full access instead.",
      "insufficient_permissions": "This token's role does not permit reading the data the integration needs. An admin or member token is recommended.",
      "unknown": "Unexpected error. Check the Home Assistant log for details."
    },
    "abort": {
      "already_configured": "This EinVault instance is already configured.",
      "reauth_successful": "Reauthentication was successful."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "EinVault options",
        "data": {
          "scan_interval": "Update interval",
          "enable_mood_sensor": "Enable the daily mood sensor",
          "include_archived": "Include archived companions"
        },
        "data_description": {
          "scan_interval": "Seconds between polls. The minimum is 60. EinVault allows 30 requests per minute per client IP, shared with every other consumer on that address.",
          "enable_mood_sensor": "Adds a mood sensor per companion. Costs one extra API request per companion on every refresh, so it is off by default.",
          "include_archived": "Create entities for companions that have been archived in EinVault."
        }
      }
    }
  }
}
